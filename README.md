# Start a django project via terminal:
    pip install django
    cd into interested directory
    python -m django startproject learningdjango
    pip install virtualenv 
    python -m virtualenv djangoenv
    
#